language: php

php:
  - "7.2"

cache:
  directories:
    - "$HOME/google-cloud-sdk/"
    - "./vendor"

dist: trusty

before_script:
  - composer self-update
  - composer install --no-interaction

script:
  - vendor/bin/phpunit
  - docker build -t hshs-domxss-scanner .

before_deploy:
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_USERNAME" --password-stdin
  - if [ ! -d "$HOME/google-cloud-sdk/bin" ]; then rm -rf $HOME/google-cloud-sdk; export CLOUDSDK_CORE_DISABLE_PROMPTS=1; curl https://sdk.cloud.google.com | bash; fi
  - source /home/travis/google-cloud-sdk/path.bash.inc
  - gcloud --quiet version
  - gcloud --quiet components update
  - gcloud --quiet components update kubectl
  - echo $GCLOUD_KEY | base64 --decode -i > ${HOME}/gcloud-service-key.json
  - gcloud auth activate-service-account --key-file ${HOME}/gcloud-service-key.json
  - gcloud --quiet config set project $GOOGLE_PROJECT_NAME
  - gcloud --quiet config set container/cluster $GOOGLE_CLUSTER_NAME
  - gcloud --quiet config set compute/zone $GOOGLE_COMPUTE_ZONE
  - gcloud --quiet container clusters get-credentials $GOOGLE_CLUSTER_NAME

deploy:
  - provider: script
    skip_cleanup: true
    on:
      branch: develop
    script: >-
      docker tag hshs-domxss-scanner siwecos/hshs-domxss-scanner:develop &&
      docker push siwecos/hshs-domxss-scanner:develop &&
      kubectl patch deployment hshs-domxss-scanner --namespace staging -p '{"spec":{"template":{"metadata":{"labels":{"date":"`date +'%s'`", "commit":"$TRAVIS_COMMIT"}}}}}'
  - provider: script
    skip_cleanup: true
    on:
      branch: master
    script: >-
      docker tag hshs-domxss-scanner siwecos/hshs-domxss-scanner:latest &&
      docker push siwecos/hshs-domxss-scanner:latest &&
      kubectl patch deployment hshs-domxss-scanner --namespace production -p '{"spec":{"template":{"metadata":{"labels":{"date":"`date +'%s'`", "commit":"$TRAVIS_COMMIT"}}}}}'
  - provider: script
    skip_cleanup: true
    on:
      tags: true
    script: >-
      docker tag hshs-domxss-scanner siwecos/hshs-domxss-scanner:$TRAVIS_TAG &&
      docker push siwecos/hshs-domxss-scanner:$TRAVIS_TAG

